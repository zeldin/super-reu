MEMORY {
  ZP:    file = "", define = yes, start = $0002, size = $001A;
  EXROM: file = %O, define = yes, start = $8000, size = $2000;
  GAME:  file = %O, define = yes, start = $A000, size = $2000;
}
SEGMENTS {
  CODE:     load = EXROM, type = ro, align = $100;
  ZEROPAGE: load = ZP,    type = zp;
}
